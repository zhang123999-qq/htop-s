# ==============================================================================
# htop-s 忽略规则
#
# 原则: 只排除「运行时不需要」的文件。
#       仓库对外只需提供 htop-s + install.sh 两个可下载文件,
#       测试资产与开发脚手架留在本地即可, 不随仓库分发。
#
# 注意: .gitignore 只对「未跟踪」文件生效。
#       已经提交过的文件必须先 git rm --cached 移出索引, 规则才会起作用。
#       确需强行提交某个被忽略的文件: git add -f <路径>
# ==============================================================================

# ------------------------------------------------------------------------------
# 1. 测试资产 —— 不进入远程仓库
# ------------------------------------------------------------------------------

# 1.1 测试目录 (整体排除)
#     包含: 集成测试套件 run-tests.sh、夹具生成器 make-mock.sh、
#           mock /proc 数据 (tests/mock/)、测试沙箱 (tests/sandbox/)
tests/

# 1.2 本地开发文档 —— 归档于 docs/internal/, 不分发
#     含: 自测报告 / 真实环境测试报告 / 修复报告 / 渲染性能分析
#     交付文档 (README / 使用手册 / 开发方案) 仍在根目录, 不在此列
docs/internal/

# 1.3 根目录散落的测试脚本 —— 防止以后随手新建时漏掉
#     覆盖 test-foo.sh / foo-test.sh / test_foo.sh / foo_test.sh / foo.test.sh
test-*.sh
*-test.sh
test_*.sh
*_test.sh
*.test.sh

# 1.4 其他常见测试产物
coverage/
.coverage
htmlcov/
*.gcov
junit*.xml

# ------------------------------------------------------------------------------
# 2. 构建脚手架
#     htop-s 是单文件成品, 无构建步骤。
#     .build/ 仅开发期用于把脚本分片拼接成 htop-s, 不属于交付物。
# ------------------------------------------------------------------------------
.build/

# ------------------------------------------------------------------------------
# 3. 运行期产物
# ------------------------------------------------------------------------------
*.log
*.pid
*.tmp.*

# ------------------------------------------------------------------------------
# 4. 发布产物
#     由 htop-s + install.sh 现场生成 (含 sha256), 不入库。
#     Release 上传时从本地 dist/ 取。
# ------------------------------------------------------------------------------
dist/

# ------------------------------------------------------------------------------
# 5. 工具与会话状态 (不属于项目源码)
# ------------------------------------------------------------------------------
.workbuddy/

# ------------------------------------------------------------------------------
# 6. 归档区 —— 已从工作目录剔除、但仍保留备查的历史文件
#     含: htop-s.v1.bak  (旧版脚本, 存在 13 项已知缺陷, 仅作对照参考)
#         build-scaffold/ (构建脚手架的历史中间产物, 内容已内嵌进 p1~p4)
#     采用归档而非删除: 不占工作目录视线, 且可随时找回。
#     确认无用后可整体删除 .archive/。
# ------------------------------------------------------------------------------
.archive/

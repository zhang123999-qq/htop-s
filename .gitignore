# 测试夹具与沙箱产物 (由 make-mock.sh / run-tests.sh 生成)
tests/mock/
tests/sandbox/
tests/quick*/

# 构建脚手架 —— htop-s 本身即唯一源文件, 无构建步骤
.build/

# 工具状态 (WorkBuddy 会话数据, 不属于项目源码)
.workbuddy/

# 旧版脚本备份 (存在 13 项已知缺陷, 仅作对照)
htop-s.v1.bak

# 运行期产物
*.log
*.pid
*.tmp.*
